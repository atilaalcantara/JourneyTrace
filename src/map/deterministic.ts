import type{Journey}from'../core/journey/types';import{createTiming}from'../core/journey/timing';import{cameraAt,positionAtDistance,type CameraMode}from'./camera';export function frameState(j:Journey,frameIndex:number,fps:number,duration:number,mode:CameraMode,viewport:{width:number;height:number}){const elapsed=frameIndex/fps,progress=Math.min(1,elapsed/duration),distance=createTiming(j,'balanced')(progress),position=positionAtDistance(j,distance);return{elapsed,progress,distance,position,camera:cameraAt(j,distance,mode,viewport)}}
