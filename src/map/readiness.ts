import type{Map}from'maplibre-gl';export async function waitForFrameReady(map:Map,timeoutMs=10000){if(map.loaded()&&map.areTilesLoaded())return;await new Promise<void>((resolve,reject)=>{const timer=setTimeout(()=>{cleanup();reject(new Error('Map tiles did not become ready'))},timeoutMs);const done=()=>{if(map.loaded()&&map.areTilesLoaded()){cleanup();resolve()}};const cleanup=()=>{clearTimeout(timer);map.off('idle',done);map.off('render',done)};map.on('idle',done);map.on('render',done)})}
