import{describe,it,expect}from'vitest';import{parseTimeline}from'../core/timeline/parser';import{frameState}from'./deterministic';const j=parseTimeline([{time:'2025-01-01T00:00Z',point:'-16.6869,-49.2648'},{time:'2025-01-01T01:00Z',point:'-16.70,-49.25'},{time:'2025-01-01T02:00Z',point:'-16.71,-49.24'}]);describe('live camera modes',()=>{it('preserves marker and route progress across mode changes',()=>{const steady=frameState(j,756,30,60,'steady',{width:900,height:600}),fixed=frameState(j,756,30,60,'fixed',{width:900,height:600}),dynamic=frameState(j,756,30,60,'dynamic',{width:900,height:600});expect(fixed.progress).toBe(steady.progress);expect(dynamic.distance).toBe(steady.distance);expect(fixed.position).toEqual(steady.position)});it('fixed is stationary while steady and dynamic follow',()=>{const a=frameState(j,300,30,60,'fixed',{width:900,height:600}),b=frameState(j,1200,30,60,'fixed',{width:900,height:600}),s=frameState(j,1200,30,60,'steady',{width:900,height:600}),d=frameState(j,1200,30,60,'dynamic',{width:900,height:600});expect(a.camera).toEqual(b.camera);expect(s.camera.center).toEqual(s.position.lng?[s.position.lng,s.position.lat]:s.camera.center);expect(d.camera.zoom).toBeGreaterThan(s.camera.zoom)})});
