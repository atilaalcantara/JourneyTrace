import type {TimelinePoint,TimelineSource}from'../timeline/types';export type Compression='off'|'gentle'|'balanced'|'strong';export type JourneyStatistics={validPointCount:number;filteredPointCount:number;totalDistanceMeters:number;start:number;end:number;spanMilliseconds:number};export type Journey={source:TimelineSource;points:TimelinePoint[];renderPath:TimelinePoint[];cumulativeDistanceMeters:number[];statistics:JourneyStatistics;removedOutliers:number};
