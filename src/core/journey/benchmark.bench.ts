import{bench,describe}from'vitest';import{parseTimeline}from'../timeline/parser';const fixture=(n:number)=>Array.from({length:n},(_,i)=>({time:new Date(1735689600000+i*60000).toISOString(),latitudeE7:515000000+(i%1000),longitudeE7:-1200000+(i%1000)}));describe('synthetic Timeline processing',()=>{for(const n of[1000,10000,100000])bench(`${n.toLocaleString()} points`,()=>{parseTimeline(fixture(n))},{time:100,iterations:1})});
