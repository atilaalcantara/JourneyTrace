import type { Coordinate } from './types'; const MAX_LAT=85.05112878;
export function parseCoordinate(value:unknown):Coordinate|null { if(typeof value==='string'){const p=value.trim().replace(/^geo:/,'').split('?')[0].replaceAll('°','').replaceAll(' ','').split(',');return p.length>=2?valid(+p[0],+p[1]):null} if(!value||typeof value!=='object')return null; const r=value as Record<string,unknown>; if('latLng'in r||'point'in r)return parseCoordinate(r.latLng??r.point); let lat=r.latitudeE7??r.latE7??r.latitude??r.lat,lng=r.longitudeE7??r.lngE7??r.longitude??r.lng; if(typeof lat==='number'&&Math.abs(lat)>1e6)lat/=1e7;if(typeof lng==='number'&&Math.abs(lng)>1e6)lng/=1e7;return valid(lat,lng) }
function valid(lat:unknown,lng:unknown):Coordinate|null{return typeof lat==='number'&&typeof lng==='number'&&Number.isFinite(lat)&&Number.isFinite(lng)&&lat>=-MAX_LAT&&lat<=MAX_LAT&&lng>=-180&&lng<=180?{lat,lng}:null}
